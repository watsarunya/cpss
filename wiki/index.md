# Index

แคตตาล็อกทุกหน้าใน wiki นี้ จัดกลุ่มตามประเภท อัปเดตทุกครั้งที่มีการ ingest หรือแก้ไขหน้าใดๆ (ดูกฎใน `CLAUDE.md` ข้อ 5)

## Sources
- [[เข้าสู่ระบบ]] — หน้า clip จากโฮมเพจ cpbf.co.th มีแบนเนอร์พันธมิตรและลูกค้า ยังไม่มีข้อความเนื้อหาที่ระบุตัวตนได้ (อัปเดตล่าสุด: 2026-07-17)
- [[Brief Redesign เว็บไซต์ cpbf.co.th]] — โจทย์ redesign เว็บไซต์จากผู้ใช้ (UI style, sitemap, e-commerce flow) (อัปเดตล่าสุด: 2026-07-17)
- [[Design Assets - Logo และ Hero Banner]] — โลโก้จริง (ยืนยันชื่อบริษัท CP B&F) และภาพ hero banner สต็อก (อัปเดตล่าสุด: 2026-07-17)
- [[CI Guideline และ Reference Design]] — CI ทางการ (สี/โลโก้/ฟอนต์/ชื่อนิติบุคคล) จาก `raw/assets/CI/` และภาพ mood reference จาก `raw/assets/ref/` (อัปเดตล่าสุด: 2026-07-17)
- [[ข่าวและกิจกรรม - รูปภาพและเนื้อหาข่าว]] — รูปข่าว 3 ไฟล์จาก `raw/assets/News/` + เนื้อหาข่าวจริง (Beanie Coffee เปิดตัว, Kaset Fair, ตรุษจีน 2568) (อัปเดตล่าสุด: 2026-07-17)
- [[Shop Online - รายการสินค้า 5 รายการ]] — สินค้าจริง 5 รายการของ cpbf.co.th พร้อม URL/description/ราคา (Konjac Jelly, Beverage Creamer, Coffee Flower Honey, House Blend Coffee, HEY! BEV) (อัปเดตล่าสุด: 2026-07-17)
- [[Our Services - เนื้อหาบริการ 4 รายการ]] — เนื้อหาบริการเต็ม 4 รายการ (OEM น้ำดื่ม, Coffee Roasting Service, บริการผลิตเครื่องดื่มครบวงจร, บริการรับจัดเลี้ยง) (อัปเดตล่าสุด: 2026-07-17)
- [[style.css - Global Style Variables]] — ไฟล์ CSS global (`design/style.css`) จากผู้ใช้ ⚠️ สีขัดแย้งกับ CI ทางการ แต่ผู้ใช้ยืนยันเก็บไว้ตามเดิม, ผูกเข้ากับ mockup แล้ว (rev.9) (อัปเดตล่าสุด: 2026-07-17)
- [[News & Contact Section - HTML+CSS]] — markup+CSS สำหรับ section News & Event (redesign) และ Contact Us (ใหม่) จากผู้ใช้ ใช้จริงใน mockup แล้ว (rev.10) โดยแทนเนื้อหาข่าว placeholder ด้วยข่าวจริงที่มีอยู่ (อัปเดตล่าสุด: 2026-07-17)
- [[Our Business Section - HTML+CSS]] — markup+CSS สำหรับ section ใหม่ "Our Business" จากผู้ใช้ ใช้จริงใน mockup แล้ว (rev.11) ยุบรวม section "ธุรกิจของเรา" เดิม + "Our Services" เดิมเป็น section เดียว (อัปเดตล่าสุด: 2026-07-17)
- [[About Us Section - HTML+CSS]] — markup+CSS สำหรับ section ใหม่ "About Us" (การ์ดกระดาษ) จากผู้ใช้ ใช้จริงใน mockup แล้ว (rev.12) แทนที่ photo panel เดิม (rev.5) + มีเนื้อหาข้อเท็จจริงใหม่เรื่องปีก่อตั้ง/เครือเจริญโภคภัณฑ์ที่ยังไม่มี source อื่นยืนยันซ้ำ, ปรับตาม screenshot จริง (2026-07-18): ตัด highlights 3 สถิติออก + เพิ่มปุ่ม "อ่านเพิ่มเติม" สี `#e91e63` (อัปเดตล่าสุด: 2026-07-18)
- [[Online Shop Section - HTML+CSS]] — markup+CSS สำหรับ section ใหม่ "Online Shop" (ดีไซน์การ์ดใหม่) จากผู้ใช้ ใช้จริงใน mockup แล้ว (rev.13) แทนที่การ์ด "SHOP ONLINE" เดิม (rev.7) — ใช้ข้อมูลสินค้าจริง 5 SKU เดิมแทนสินค้าสมมุติในตัวอย่าง, ตัดปุ่ม "+ เพิ่มลงตะกร้า" ออก, ปรับเพิ่ม (2026-07-18): กลับไปเป็น slider แสดง 4 การ์ด default เลื่อนดูใบที่ 5 เหมือน rev.7 เดิม แต่คงดีไซน์การ์ดใหม่ไว้ (อัปเดตล่าสุด: 2026-07-18)
- [[Hero Banner Section - HTML+CSS]] — markup+CSS สำหรับ section ใหม่ "Hero Banner" (polaroid gallery) จากผู้ใช้ ใช้จริงใน mockup แล้ว (rev.14) แทนที่ hero เดิม (rev.1-4, พื้นน้ำเงิน collage) — ใช้รูปจริง 4 รูป `hero-business-01.png`-`04.png` (มีอยู่แล้วใน `raw/assets/image/`), เปลี่ยน `id="hero"` เป็น `id="home"` (อัปเดตล่าสุด: 2026-07-18)

## Entities
- [[cpbf.co.th (บริษัท)]] — **CP B&F** ชื่อนิติบุคคลเต็ม "บริษัท ซีพี บีแอนด์เอฟ (ไทยแลนด์) จำกัด" บริษัทกลุ่มอาหาร/เครื่องดื่ม ก่อตั้งปี 2016 ส่วนหนึ่งของเครือเจริญโภคภัณฑ์ (⚠️ ยังไม่มี source อื่นยืนยันซ้ำ) สายธุรกิจ OEM Manufacturing, Catering, Product, Café + Brand System ทางการ (สี/โลโก้/ฟอนต์) + แบรนด์ย่อย Beanie Coffee + ชื่อ CEO + สินค้าจริง 5 SKU พร้อมราคา + บริการเต็ม 4 รายการ ⚠️ style.css ใหม่ใช้สีต่างจาก CI ทางการ (ผู้ใช้ยืนยันตั้งใจ) ⚠️ ยังไม่มีข้อมูลติดต่อจริง (email/เบอร์โทร/ที่อยู่) (อัปเดตล่าสุด: 2026-07-17)

## Concepts
- [[โครงการ Redesign เว็บไซต์ cpbf.co.th]] — โปรเจกต์ redesign เว็บไซต์ cpbf.co.th: UI style guide, site map, e-commerce flow (อัปเดตล่าสุด: 2026-07-17)
- [[Wireframe หน้าแรก (Redesign cpbf.co.th)]] — wireframe หน้าแรก rev.18 layout bold bento, section เกี่ยวกับเรา (เดิม rev.5 ภาษาอังกฤษ) ถูกแทนที่ด้วย "About Us" การ์ดกระดาษ (rev.12, ยังภาษาอังกฤษ ⚠️ ภาษาไม่สม่ำเสมอทั้งหน้ายังคงอยู่ ⚠️ มีข้อเท็จจริงใหม่เรื่องปีก่อตั้ง/เครือ CP ที่ยังไม่ยืนยันซ้ำ ⚠️ เปลี่ยนสีจาก CI เป็นชุดสี style.css), section ข่าวสารและกิจกรรม redesign ใหม่ (BEM, CSS ใน style.css) ด้วยรูป/เนื้อหาข่าวจริง 3 รายการ, section ช้อปสินค้าเปลี่ยนเป็น "Online Shop" ดีไซน์การ์ดใหม่ สินค้าจริง 5 รายการ (rev.13, คง slider แสดง 4 การ์ด เลื่อนดูใบที่ 5 เหมือน rev.7 เดิม ตามที่ผู้ใช้ขอเพิ่ม ⚠️ ตัดปุ่ม "+ เพิ่มลงตะกร้า" ออก), ยุบรวม section "ธุรกิจของเรา" เดิม + "Our Services" เดิมเป็น section เดียว "Our Business" (การ์ด 4 ใบ เนื้อหาบริการจริงเดิม ⚠️ ตัด stats-strip ออก, ⚠️ ยังไม่ยืนยันการตีความยุบรวมกับผู้ใช้), เพิ่ม section ใหม่ "Contact Us" (⚠️ ข้อมูลติดต่อยังเป็น placeholder ยกเว้นโดเมนจริง www.cpbf.co.th), section hero "Hero Banner" (`id="home"`) เริ่มจาก polaroid gallery รูปจริง 4 รูป (rev.14) → ตัด border/eyebrow/ปุ่ม CTA แทน gallery ด้วยแบนเนอร์ภาพเดียว (rev.16) → **rev.17: แยกสลายแบนเนอร์เป็น layer ข้อความ/ไอคอน/รูปภาพแยกกัน (`.hero-showcase`) พร้อม parallax animation ตาม mouse/scroll (JS ตัวแรกในไฟล์นี้), ใช้รูปสินค้าไดคัท `New Project.png`, BG เปลี่ยนเป็น `var(--primary)` (`#1B5EF9`)** (⚠️ เนื้อหายังเป็นสื่อโปรโมทสินค้า "Thai Specialty Coffee" แบรนด์ "ATO Chiang Rai" ไม่ใช่เนื้อหาของ CP B&F เอง ยังไม่ยืนยันว่าใช้ชั่วคราวหรือถาวร ⚠️ ไม่ได้ทำกราฟิกเจดีย์/วัดจากภาพ ref เพราะไม่มี asset แยก) → **rev.18: จัดตำแหน่ง/ขนาด font/layout ของ `.hero-showcase` ใหม่ทั้งหมดด้วย absolute positioning (%) อิงพิกัดจริงที่วัดจากภาพ ref (1376×702px) ผ่าน Python/Pillow/numpy แทน flexbox เดิม, ล็อกสัดส่วนด้วย `aspect-ratio:1376/702`, font-size ใช้ `clamp()`+`cqw` สเกลตาม container, ลบ `.hero-decoration` (จุด/pill 4 ชิ้นจาก rev.1-4) ที่ไม่มีในภาพ ref ออก** (⚠️ ยังไม่ได้ตรวจสอบด้วยเบราว์เซอร์จริง) → **ปรับเพิ่ม (rev.18.1): ลดขนาดรูปสินค้า (`width:36%`→`26%`) + จัดกึ่งกลางแนวนอนด้วย `left:37%` กันไม่ให้ทับ description/badges** → **rev.19: เพิ่ม `.hero-slider`/`.hero-slider__track`/`.hero-slider__slide` ห่อ `.hero-showcase` ให้รองรับหลายแบนเนอร์เลื่อนได้ (scroll-snap, ล้อแพทเทิร์น `.shop-section__slider` เดิม), เพิ่ม slide ใหม่ index "01" (เนื้อหา/รูปซ้ำกับ slide "02" เดิมชั่วคราว), เพิ่มปุ่มเลื่อน `.hero-slider__nav`, แก้ parallax script เป็น `querySelectorAll` รองรับหลาย slide** (⚠️ ยังไม่ได้ตรวจสอบด้วยเบราว์เซอร์จริง) → **ปรับเพิ่ม (rev.19.1): slide 01 เปลี่ยนรูปสินค้าเป็น `New Project1.png` + เปลี่ยนข้อความ "Chiang Rai"→"Chiang Mai" ทุกจุด (slide 02 ไม่แตะต้อง เนื้อหาต่างกันแล้ว)**, **header/nav เปลี่ยนเป็น overlay ทับ hero + เมนู 5 รายการชิดขวา + ไอคอน login/cart/language (rev.15, ⚠️ ไม่ sticky ตลอดหน้าอีกต่อไป ⚠️ "ธุรกิจของเรา"/"บริการของเรา" ชี้ anchor เดียวกัน) — **ปรับเพิ่ม (rev.15.1): พื้นหลัง header เปลี่ยนจากโปร่งใสเป็นสีขาวตัน + ขยายโลโก้เป็น 56px + สีตัวอักษร/ไอคอนเปลี่ยนเป็นเข้มให้ contrast + ขยาย hero padding-top กัน header ทับเนื้อหา****, ไฟล์ mockup ที่ `design/homepage-wireframe.html` ผูกกับ `design/style.css` (⚠️ style.css ใช้สีต่างจาก CI ทางการ ผู้ใช้ยืนยันตั้งใจ — ตอนนี้เหลือเฉพาะ footer + hero background จุดที่ยังใช้ CI ทางการ) → **rev.20: ปรับ padding/margin ทุก `<section>` (7 จุด: hero/about/business/shop/news/contact/cta-banner) ให้เท่ากันหมด — ลบ margin ที่มีอยู่จุดเดียว (`.cta-banner`), padding เป็น `50px` เท่าทุกด้านทั้งหมด (เดิมส่วนใหญ่ `96px 32px 112px`)** (⚠️ เสี่ยง header ทับ hero เพราะลด padding-top จาก 96px ที่ตั้งใจกันไว้ตั้งแต่ rev.15.1, ⚠️ ระยะห่างระหว่าง section แน่นขึ้นมาก ยังไม่ได้ตรวจสอบด้วยเบราว์เซอร์จริง) → **rev.21: ลบ `.cta-banner` section (พื้นแดง "พร้อมสั่งซื้อหรือร่วมงานกับเรา") ออกทั้งหมด** พร้อม CSS/dead code `.btn-solid`/`.doodle` ตามคำสั่งผู้ใช้ แก้ปัญหา "CTA ซ้ำซ้อน" ที่ค้างมาตั้งแต่ rev.10 → **rev.22: เพิ่ม section ใหม่ "Our Partners" (`.partners-section`) เหนือ Online Shop section ใช้โลโก้ทั้งหมด 12 ไฟล์จาก `raw/assets/image/Cients` (ตรวจสอบทีละไฟล์แล้วเป็นแบรนด์เครื่องชงกาแฟ/อุปกรณ์กาแฟ: Saeco, Casadio, Evoca Group, Hiway, Rancilio, Gaggia Milano, Nuova Simonelli, TCN, EGRO, NECTA, Dr.Coffee, Cunill)** (⚠️ ไม่ตรงกับภาพ ref ที่ผู้ใช้ส่งมาซึ่งเป็นโลโก้สื่อ/สำนักพิมพ์สไตล์ "As seen in:" — ตีความภาพ ref เป็นแค่ตัวอย่างเลย์เอาต์เท่านั้น ยังไม่ได้ยืนยันกับผู้ใช้) → **rev.23: เพิ่ม `margin-bottom: 0;` ระบุชัดเจนให้ทุก `<section>` ทั้ง 7 จุด** ตามคำสั่งผู้ใช้ (ค่า default เดิมก็เป็น 0 อยู่แล้ว เป็นการระบุชัดเจนกันความคลุมเครือ ไม่เปลี่ยนภาพจริง) → **rev.24: h2 title ทั้ง 6 จุด (`.web-title` ×5 + `.contact-section__title` ×1) เปลี่ยนจาก responsive `clamp()` เป็นค่าคงที่ `font-size: 2.5rem` เท่ากันทุกจอ** → **rev.25: รวมฟอนต์ทั้งหน้าเป็น IBM Plex Sans Thai ตัวเดียว** — เปลี่ยน `--font-family` เป็น `"IBM Plex Sans Thai", sans-serif` (เดิม Inter/Kanit), ตัด Bricolage Grotesque ออกจาก h1/h2 rule และ Google Fonts import ทั้งหมด, เพิ่ม weight 800 ให้ import (⚠️ ยังไม่ตรวจสอบด้วยเบราว์เซอร์จริง) → **rev.26: เปลี่ยน `--accent-pink` จาก `#e91e63` เป็น `#e975cd`** (ตรงกับสี `--pink` ของ CI ทางการพอดี) → **rev.27: ปรับ logo strip "Our Partners" เป็นแถวเดียวเลื่อนได้ — `.partners-section__logo` height `40px`→`100px` (mobile `30px`→`70px`), `.partners-section__logos` จาก `flex-wrap:wrap` เป็น `flex-wrap:nowrap`+`overflow-x:auto`+`justify-content:flex-start` (ซ่อน scrollbar ตามแพทเทิร์น `.shop-grid`)** → **rev.28: ตัดการ์ดกระดาษ "About Us" (rev.12) ออกเกือบทั้งหมด — ลบพื้นหลัง section, กล่องการ์ด, ป้าย "About Us"/"Our Story"/ป้ายกลม "CP B&F"/แถบคั่น เหลือ h2+2 ย่อหน้า+ปุ่ม "อ่านเพิ่มเติม" จัดกึ่งกลางแบบ plain text ตามภาพ ref (⚠️ การลบป้าย 2 จุด+แถบคั่น+เปลี่ยน alignment เป็น judgment call ยังไม่ยืนยันกับผู้ใช้)** → **rev.29: แทนที่ section "Online Shop" (slider, rev.13) ด้วย section ใหม่ "Products" (`id="products"`) — grid static 4 คอลัมน์ + quantity stepper + ปุ่ม "Add to cart" + toggle "View more", ใช้สินค้าจริง 5 รายการเดิมเท่านั้น (ไม่ fabricate ให้ครบ 8 ใบตาม template ที่ผู้ใช้ส่งมา), รูปสินค้าหมุนเวียน `New Project.png`/`1`/`2` ชั่วคราว, สี token ใหม่ alias ทับของเดิมตาม CI แทนสีทองจาก template (⚠️ มีแค่ 5/8 การ์ด, ⚠️ รูปสินค้ายัง placeholder, ⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง)** → **rev.30: restyle CSS-only ของ Products section ตามภาพ ref apéritif brand "What's in Season" — ตัดกล่องการ์ด (bg/border/shadow/radius) ออกหมดเหลือโปร่งใสกึ่งกลาง, เพิ่มรูปทรงโค้งประตู (arch) หลังรูปสินค้าด้วย `::before`, เพิ่มฟอนต์ serif "Playfair Display" (`--font-serif`) ให้ heading+ชื่อสินค้า, ซ่อนบรรทัดราคาแยก, ปุ่ม "Add to cart" เปลี่ยนเป็นมุมมนเล็กน้อย+เต็มความกว้าง+ตัวพิมพ์ใหญ่, ย้าย hover จากการ์ดไปที่รูปสินค้า (⚠️ judgment call: คงสีปุ่มเป็น CI blue ไม่เปลี่ยนเป็นทองตามภาพ ref, ⚠️ judgment call: คงจำนวนคอลัมน์/การ์ด 5 ใบเดิมไม่เปลี่ยนตาม screenshot ที่อาจถูก crop, ⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง)** → **rev.30.1: ปรับเพิ่มตามคำสั่งย่อย — `.product-card__visual` ตัด height ออก + max-width 220px→300px, `.product-card__description` ตัดคำอัตโนมัติ 2 บรรทัด (`-webkit-line-clamp`), ปุ่ม "Add to cart" เปลี่ยนสไตล์ให้ตรงกับ `.btn-primary` ทั้งหมด (pill/shadow/hover lift), ลบ `.product-heading__description` และ label "OUR PRODUCTS" (`.product-heading__eyebrow`) ออกทั้งคู่ (⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง)** → **rev.30.2: ชื่อสินค้าจำกัด 1 บรรทัด (ellipsis), เลิกใช้ฟอนต์ serif "Playfair Display" กลับเป็น IBM Plex Sans Thai ทั้งหมด, จัดปุ่ม "Add to cart" ให้อยู่ระดับเดียวกันทุกการ์ดด้วย `height:100%`+`margin-top:auto` (⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง)** → **rev.31: recolor `.contact-section` (Contact Us) + `<footer>` ให้ใช้สี CI ทางการตามภาพ ref "Mariana" เดิม — เพิ่ม token `--ci-blue`/`--ci-yellow`/`--ci-red`, เปลี่ยนพื้นเหลืองครีมเป็นเหลืองสด, เปลี่ยน `.contact-panel` จากชมพูเป็นแดง (จุดเปลี่ยนเด่นสุด), ให้ `<footer>` (เดิมไม่มี CSS เลย) มีพื้นน้ำเงิน CI ต่อเนื่องจาก bottom bar เป็นแถบเดียวกัน — ไม่แตะ HTML/layout, ตอนนี้ footer+contact-section ใช้ CI ทางการร่วมกัน 2 จุด (⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง)** → **rev.31.1: ปรับ padding ตามคำสั่งเพิ่มเติม + เปลี่ยน notation สี — `.contact-section` ตัด `padding:50px` (rev.20) เหลือ `padding:0` (ข้อยกเว้นแรกจาก convention rev.20), `.contact-section__footer` padding บน/ล่างจาก `34px`→`20px` (ซ้าย/ขวาคง responsive เดิม, ปรับ mobile override ให้เท่ากันทุกด้านตาม), `--ci-yellow` เปลี่ยน notation จาก `#FFE02F`→`rgb(255 224 47)` (ค่าสีเดียวกัน), `--ci-red` ตรวจแล้วตรงกับ `#FF242A` เดิมอยู่แล้ว ไม่มีการเปลี่ยนแปลง (⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง)** → **rev.32: restyle section "Our Business" ตามภาพ ref ใหม่ (4 คอลัมน์ไอคอนวงกลมสี + หัวข้อ + คำอธิบาย คั่นเส้นแนวตั้ง พื้นครีม) — เปลี่ยนจากการ์ดกล่อง stagger เดิม (rev.11) เป็นแถบไอคอน flat, ตัดปุ่ม "ดูรายละเอียด"+เลขลำดับ 01-04 ออก, ไอคอน emoji เดิม (☕💧🥤🍽) ใส่ในวงกลมทึบสี CI (blue/yellow/red สลับตามภาพ ref), พื้นหลัง section เป็นครีม `#FBF7EF`, คงเนื้อหาบริการจริง 4 รายการเดิมทั้งหมด (⚠️ สีไอคอนสดกว่าโทนพาสเทลในภาพ ref ยังไม่ยืนยันกับผู้ใช้, ⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง)** → **rev.33: BG `.about-section` เปลี่ยนเป็น `#EBEAE7` + restyle ปุ่ม CTA จริงทั้งหน้า 5 จุด (`.btn-primary`/`.btn-accent`/`.product-card__button`/`.contact-panel__button`/`.contact-section__footer-button`) จาก pill/shadow เป็นตัวหนังสือขีดเส้นใต้ตามภาพ ref "SEE WHAT'S INSIDE" (ยืนยันขอบเขตกับผู้ใช้ผ่าน `AskUserQuestion` ก่อนแก้: BG→About Us, ปุ่ม→global ทั้งหน้า) — ไม่แตะปุ่ม UI เชิงฟังก์ชัน (header icon/quantity stepper/slider nav/view-more) (⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง)** → **rev.34: 3 จุดพร้อมกัน — Our Business ตัด eyebrow "What we do" + BG เปลี่ยนเป็นขาว `#ffffff`, ย้าย section "Our Partners" ไปไว้ใต้ Products (ลำดับใหม่ hero→about→business→product→partners→news→contact) + ตัด eyebrow + เปลี่ยน title "พันธมิตรของเรา"→"Our Partners", Products เปลี่ยน title "What's in Season"→"Ready to Shop?" + ลด `.product-heading` margin-bottom เหลือ `30px`** (⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง) → **rev.35: header ทุก section ให้ font-size คงที่ `2.5rem` เท่ากันหมด — พบ `.product-heading__title` เป็นจุดเดียวที่ตกหล่นยังใช้ `clamp()` (Products section เพิ่มทีหลัง rev.24) แก้ให้ตรงกัน, ตรวจสอบ `.product-card__name` พบว่าจำกัด 1 บรรทัด+ellipsis ไว้แล้วตั้งแต่ rev.30.2 ไม่ต้องแก้เพิ่ม, restyle ปุ่ม `.product-view-more__button` จาก pill เต็มเป็นสไตล์ text-link ขีดเส้นใต้เดียวกับ `.btn-accent`/`.about-card__button` (rev.33 pattern)** (⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง) → **rev.36: Products section — ตัด quantity stepper (+/-) ออกทั้ง 5 การ์ด แทนที่ด้วยราคา `฿{จำนวนเต็ม}` + ปุ่มวงกลม "+" (`.product-card__add-button`) ตามภาพ ref, `.product-card` เพิ่ม `max-width:300px` ให้ทุกการ์ดกว้างเท่ากัน, ปุ่ม View more ตัดไอคอน chevron ออก, เปลี่ยนสี `--product-bg` เป็น token CI เรียง `--ci-blue`/`--ci-yellow`/`--accent-pink`/`--ci-blue` ตามที่สั่ง** (⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง) → **rev.37: ปุ่ม `.product-card__add-button` เปลี่ยนสีเป็น `var(--ci-blue)`+ตัวอักษร/ไอคอนขาว, BG `.about-section` เปลี่ยนจาก `#ebeae7`→`#f4f3f1`, เพิ่มขอบล่าง Hero Banner แบบ Scallop/Stamp Edge ด้วย `.hero-section::after` (radial-gradient รัศมี 16px สีตรงกับ bg `.about-section` ที่อยู่ถัดไป)** → **rev.37.1: ผู้ใช้สั่ง flip scallop กลับด้าน — ย้ายไปเป็น `.about-section::before` ห้อยลงจาก Hero สีน้ำเงินแทน (เดิม About Us สีเทาโผล่ขึ้น), พร้อมแก้บั๊กเดิม `.hero-section` ใช้ `var(--primary)` ที่ไม่มีอยู่จริงเป็น `var(--ci-blue)`** → **rev.37.2: ผู้ใช้เห็นว่า scallop ไม่สวย สั่งลบทิ้งทั้งหมด กลับเป็นขอบตรงธรรมดา (คงการแก้บั๊ก `--primary` ไว้)** → **rev.38: redesign `<footer>` เต็มรูปแบบตามภาพ ref แบรนด์กาแฟ "MORNCOFFEE" — จากบรรทัด copyright เดียวพื้นน้ำเงิน (rev.31) เป็น 3 คอลัมน์ (Contacts/Our Business+Website/nav+social badge) + wordmark "CP B&F" ตัวใหญ่เต็มความกว้าง + bottom bar (copyright+legal links), เนื้อหาปรับให้ตรงกับ CP B&F จริงแทนเนื้อหาร้านกาแฟใน ref (⚠️ Contacts ยัง placeholder, ⚠️ social handle ยังไม่ยืนยัน)** → **rev.38.1: `.site-footer__wordmark` font-size เปลี่ยนจาก `clamp()` เป็นค่าคงที่ `7rem`, ลบส่วน `.contact-section__main` ของ Contact Us section ทิ้งทั้งหมด (intro/cta/contact-panel) คงเหลือแค่ `.contact-section__footer` เดียว พร้อมลบ CSS dead code ที่เกี่ยวข้องทั้งหมด** → **rev.38.2: `.site-footer__wordmark` ข้อความเปลี่ยนจาก "CP B&F" เป็น "CP B&F Company Limited"** (⚠️ เสี่ยงล้นที่ font-size คงที่ 7rem ยังไม่ตรวจ, ⚠️ ยังไม่ตรวจด้วยเบราว์เซอร์จริง) (อัปเดตล่าสุด: 2026-07-20)
